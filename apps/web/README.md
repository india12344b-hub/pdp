# PDP Web — Foundation v1

Run from this folder:

`npm.cmd install`

then:

`npm.cmd run dev`

This is the visual homepage foundation. D1, R2, authentication, candidate profiles and recruiter features will be added in later phases.
