import './globals.css';

export const metadata = {
  title: 'PDP — Professional Digital Profile',
  description: 'Your Performance Display Platform — Where Skills Are Visually Verified.'
};

export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>;
}
